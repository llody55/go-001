server:
  port: 18080
database:
  # 本地 SQLite 数据库文件，开箱即用；需要 MySQL 时由部署层替换 dialect/dsn
  dsn: "./data/metro.db"
  # 读写分离机制（主/从两个连接），单机 SQLite 默认指向同一文件，部署层可换从库 dsn
  enable_replica: true
jwt:
  # 占位符，运行时由环境变量 APP_JWT_SECRET 注入；未注入时进程启动生成一次性密钥
  secret: "CHANGE_ME"
  expire_hours: 12
